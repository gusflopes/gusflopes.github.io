---
import { getCollection, render } from 'astro:content';
import type { CollectionEntry } from 'astro:content';
import Default from '../../../layouts/Default.astro';
import { InsightArticlePage } from '../../../components/pages/InsightArticlePage';
import { formatDatePtBR } from '../../../lib/format';

export async function getStaticPaths() {
  const entries = await getCollection('insights');
  return entries.map((entry) => ({ params: { id: entry.id }, props: { entry } }));
}

interface Props {
  entry: CollectionEntry<'insights'>;
}

const { entry } = Astro.props;
const { Content } = await render(entry);
const { title, excerpt, category, date, duration, image } = entry.data;
---

<Default
  title={title}
  description={excerpt}
  image={image}
  type="article"
  publishedDate={date}
>
  <InsightArticlePage
    title={title}
    excerpt={excerpt}
    category={category}
    dateFormatted={formatDatePtBR(date)}
    duration={duration}
    image={image}
    client:load
  >
    <Content />
  </InsightArticlePage>
</Default>
